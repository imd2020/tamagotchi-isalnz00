function setup() {
  createCanvas(600, 500);
  frameRate(30);
}

window.addEventListener("resize", function () {
  resizeCanvas(600, 500);
  clear();
});

new p5();
var width = 600;
var height = 500;
